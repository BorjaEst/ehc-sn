# =============================================================================
# Metadata (comments only)
# class: benchmark-manifest
# id: arena-struct-tem-v2
# role: artifact manifest for model-comparison benchmark loading and provenance
# =============================================================================

schema_version = "1"
artifact_id = "arena-struct-tem-v2-default-vram8gib"
artifact_type = "model_core"
model_family = "tem-v2"
model_config_path = "config/models/tem-v2-base.toml"
checkpoint_path = "checkpoints/tem-v2/last.ckpt"
checkpoint_format = "lightning"
supported_tracks = ["arena-struct"]

[load_policy]
freeze_core = true
allow_full_resume = false

[benchmark_policy]
mode = "model-comparison"

[provenance]
source_run = "ready-track-default"
