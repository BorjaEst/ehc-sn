# =============================================================================
# Metadata (comments only)
# class: benchmark-launcher
# id: mazehard-delib
# role: launcher defaults selecting track, manifest, and recipe entrypoints
# =============================================================================

track = "mazehard-delib"
manifest = "config/benchmarks/manifests/mazehard-delib-hrm-v1.toml"
recipe = "config/benchmarks/recipes/mazehard-delib.toml"
fixed_recipe = "default"
seed_count = 1