# =============================================================================
# Metadata (comments only)
# class: benchmark-suite
# id: paper-mazehard-delib-ready
# role: curated suite binding tracks, recipes, and artifact manifests for reporting
# =============================================================================

schema_version = "1"
suite_id = "paper-mazehard-delib-ready"
track_ids = ["mazehard-delib"]
recipe_paths = ["config/benchmarks/recipes/mazehard-delib.toml"]
fixed_recipe_labels = ["mazehard-delib-hrm-v1"]
artifact_manifest_ids = [
  "mazehard-delib-hrm-v1-default-vram8gib",
  "mazehard-delib-hrm-v2-default-vram8gib",
]
artifact_manifest_paths = [
  "config/benchmarks/manifests/mazehard-delib-hrm-v1.toml",
  "config/benchmarks/manifests/mazehard-delib-hrm-v2.toml",
]
notes = "MazeHard ehc-v1 paper inclusion is intentionally omitted until a recipe-backed config slice is added."
