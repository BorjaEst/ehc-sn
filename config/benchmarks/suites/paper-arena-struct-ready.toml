# =============================================================================
# Metadata (comments only)
# class: benchmark-suite
# id: paper-arena-struct-ready
# role: curated suite binding tracks, recipes, and artifact manifests for reporting
# =============================================================================

schema_version = "1"
suite_id = "paper-arena-struct-ready"
track_ids = ["arena-struct"]
recipe_paths = ["config/benchmarks/recipes/arena-struct.toml"]
fixed_recipe_labels = ["arena-struct-tem-v1"]
artifact_manifest_ids = [
  "arena-struct-tem-v1-default",
  "arena-struct-tem-v2-default-vram8gib",
  "arena-struct-ehc-v1-default",
]
artifact_manifest_paths = [
  "config/benchmarks/manifests/arena-struct-tem-v1.toml",
  "config/benchmarks/manifests/arena-struct-tem-v2.toml",
  "config/benchmarks/manifests/arena-struct-ehc-v1.toml",
]
