# =============================================================================
# Metadata (comments only)
# class: benchmark-recipe
# id: arena-struct
# role: comparison protocol and execution policy for benchmarked artifacts
# =============================================================================

schema_version = "1"
recipe_id = "arena-struct-tem-v1"
track_id = "arena-struct"
mode = "model-comparison"
capability_kind = "arena_replay"
compared_artifact_type = "within-task architecture comparison"
fixed_recipe_label = "arena-struct-tem-v1"
adaptation_protocol_id = "bridge-variational-v1"
primary_metric = "accuracy_revisit"
supported_model_families = ["tem-v1", "tem-v2", "ehc-v1"]

[artifact_rules]
require_frozen_core = true
require_fresh_bridge = true

[bridge]
adapter = {}

[execution]
seed = 7
reseed_before_evaluation = true
max_batches = 0

[data]
dataset_path = "data/processed/arena/default/v1"
split = "test"
batch_size = 1
n_cases = 0
sample_ids = []

[adaptation]
objective_token_loss = "softmax_cross_entropy"

[adaptation.protocol]
examples = 16
steps = 4
optimizer = "adam"
learning_rate = 0.001
weight_decay = 0.0
betas = [0.9, 0.98]
seed = 7
reseed_before_adaptation = true
stopping_rule = "fixed_steps"

[reporting]
fixed_recipe = "arena-struct-tem-v1"

[bindings.tem-v1]
adapter = { observation_dim = 45, action_count = 4 }

[bindings.tem-v2]
adapter = { observation_dim = 45, action_count = 4 }

[bindings.ehc-v1]
adapter = { observation_dim = 45, action_count = 4 }
