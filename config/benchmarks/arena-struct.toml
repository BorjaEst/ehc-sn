# =============================================================================
# Metadata (comments only)
# class: benchmark-launcher
# id: arena-struct
# role: launcher defaults selecting track, manifest, and recipe entrypoints
# =============================================================================

track = "arena-struct"
manifest = "config/benchmarks/manifests/arena-struct-tem-v1.toml"
recipe = "config/benchmarks/recipes/arena-struct.toml"
fixed_recipe = "default"
seed_count = 1