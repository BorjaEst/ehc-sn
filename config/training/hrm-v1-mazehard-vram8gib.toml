# Training config — MazeHard × HRM-v1 (nested experiment schema)
# =============================================================================
# Based on: config/training/hrm-v1-mazehard-vram8gib.toml
# =============================================================================

[model]
    model_config_path = "config/models/hrm-v1-base.toml"

[model.components.adapter]
    encoder_kind = "rope"
    vocab_size   = 6

[model.components.controller]
    max_halt_steps    = 16
    exploration_prob  = 0.1

[model.components.objective]
    token_loss = "stablemax_cross_entropy"

[training.optimizer]
    lr           = 1e-4
    weight_decay = 0.1
    betas        = [0.9, 0.95]

[training.runtime.validation]
    max_rollout_steps = 16

[data]
    dataset_path         = "data/processed/mazehard/default/v1"
    global_batch_size    = 100
    num_workers          = 4
    prefetch_factor      = 2
    pin_memory           = true
    persistent_workers   = true
    augment              = true
    seed                 = 42

[trainer]
    accelerator         = "gpu"
    strategy            = "ddp"
    devices             = 1
    num_nodes           = 1
    precision           = "16-mixed"
    max_steps           = 50000
    val_check_interval  = 1000
    log_every_n_steps   = 10
    enable_progress_bar = true
    limit_val_batches   = 1.0
    seed                = 42

[checkpointing]
    supervised_only_warmup_steps = 500
    init_weights_groups          = ["all"]
    diagnostic_level             = "standard"
    non_finite_policy            = "raise"
    checkpoint_every_eval        = false

[checkpointing.scheduler]
    warmup_steps = 2000
    min_ratio    = 1.00

[checkpointing.target_network]
    enabled = false

[logging]
    save_dir  = "logs"
    name      = "hrm-v1-mazehard"
    log_graph = false
    prefix    = ""
