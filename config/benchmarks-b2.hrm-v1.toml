base_config_path = "hrm-mazehard.v1.toml"

[benchmark]
benchmark_id = "b2"
model_id = "hrm_only"
variant_id = "hrm-only"
baseline_family = "hrm_only"
adapter_kind = "unavailable"
availability = "blocked"
blocking_reason = "No dungeon one-shot adapter exists for the HRM-only recurrent baseline in the current repo."
compute_budget_supported = true
results_dir = "outputs/benchmarks/b2"